services:
  rcsvision:
    build: .
    container_name: rcsvision-app
    ports:
      - "3001:3001"
    volumes:
      - ./exports:/app/exports
    environment:
      - NODE_ENV=production
      - PORT=3001
      - DEV=true
    env_file:
      - .env
    restart: unless-stopped
